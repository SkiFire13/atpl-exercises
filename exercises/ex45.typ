#import "../common.typ": *

// Note 15 ex 2
#exercise[
  Show a derivation of the judgment $emptyset tack.r (fn r:{ell: Nat}. r.ell + 2) {ell = 0, ell' = 1} : Nat$ that uses an instance of the rule (ARROW), and a second derivation of the same judgment that does not use that rule.
]
#solution[
  
]
